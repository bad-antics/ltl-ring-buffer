// examples/demo.ltl — runnable demo for ltl_ring_buffer

import std::io
use ltl_ring_buffer as lib

fn main() {
    io::println("ltl_ring_buffer demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}